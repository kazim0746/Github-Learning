git config --global user.email "mahtabkazim7@gmail.com"
git config --global user.name "Mahtab Kazim"

Learn git Part-1:

1. git init
2. git add [filename] || -A
3. git status
4. git commit -m "msg"
5. git remote add origin reponame
6. git push origin master
7. git clone reponame
8. git pull origin master